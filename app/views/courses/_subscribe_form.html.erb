<% unless current_user.class == Tutor.first.class %>
  <div id="follow_form">
    <% if current_user.subscribing?(@course) %>
      <%= render 'unsubscribe' %>
    <% else %>
      <%= render 'subscribe' %>
    <% end %>
  </div>
<% end %>