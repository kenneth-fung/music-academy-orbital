<% provide(:title, "Course Creation") %>
<% provide(:url_path, courses_path(@course)) %>
<% provide(:button_text, "Create Course") %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1>Create a New Course</h1>
    <%= render 'course_form' %>
  </div>
</div>
