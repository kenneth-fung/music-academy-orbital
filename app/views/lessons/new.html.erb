<% provide(:title, "New Lesson") %>
<% provide(:url_path, lessons_path(@lesson)) %>
<% provide(:submit_text, "Publish") %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1>New Lesson for <%= @course.title %></h1>
    <%= render 'lesson_form' %>
  </div>
</div>
