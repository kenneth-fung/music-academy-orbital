<% provide(:title, 'Tutors') %>

<div class="container">
  <h1><%= @title %></h1>
  <div class="row">
    <div class="row">
      <%= render @tutors %>
    </div>
  </div>
</div>

<%= will_paginate @tutors, class: "pagination" %>
