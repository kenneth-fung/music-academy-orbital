<% provide(:title, 'Settings') %>
<% provide(:url_path, student_path(@student)) %>
<% provide(:label_prefix, 'New ') %>
<% provide(:button_text, 'Save Changes') %>

<h1>Account Settings</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'shared/user_form', user: @student %>
  </div>
</div>
